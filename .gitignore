# Dependencias
node_modules

# Build
dist

# Variables de entorno / secretos (NUNCA subir)
.env
.env.*
!.env.example

# Llaves de service account de Google (NUNCA subir)
**/soulbrew-*.json
**/*service*account*.json
scripts/wallet/sa.json

# Páginas locales con tokens de save (NUNCA subir)
scripts/wallet/*.html

# Config local de Claude Code
.claude/settings.local.json

# Sistema
.DS_Store
desktop.ini
Thumbs.db

# Editor
.vs/
